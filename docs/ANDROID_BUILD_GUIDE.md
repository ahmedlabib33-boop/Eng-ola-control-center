# Android Build Guide

Run:

```powershell
.\BUILD_ANDROID_APK.ps1
.\BUILD_ANDROID_AAB.ps1
```

Requirements:
- Python 3.11+
- Flet build dependencies
- Android toolchain as required by Flet

Scripts preserve `data/` and source folders.
