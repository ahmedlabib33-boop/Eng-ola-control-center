# AI Guardrails

- The app remains useful without cloud AI.
- Current AI service uses rule-based fallback over SQLite records.
- Responses include source, confidence, missing-data warning, and fact/inference separation.
- Private My Day data is excluded unless explicitly requested by an authorized private context.
- AI-extracted meeting actions are draft-only and require user review before saving.
