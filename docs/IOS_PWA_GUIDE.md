# iPhone Web/PWA Guide

Run web mode:

```powershell
.\RUN_FLET_WEB.ps1
```

Deploy the web build to an HTTPS host. On iPhone Safari, open the HTTPS URL and choose **Add to Home Screen**.

Native iOS packaging requires macOS and Apple signing.
